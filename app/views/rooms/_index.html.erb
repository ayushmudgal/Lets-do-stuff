<p id="notice"><%= notice %></p>

<h2> <%= @cateroyNam %> Room</h2>

<table>

  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td>&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp<td>
        <td><%= link_to (room.name), room %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Room', new_room_path %><br>
